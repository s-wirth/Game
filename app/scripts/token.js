window.Token = function(color, symbol) {
  this.color = color;
  this.symbol = symbol;
}

Token.SYMBOLS = ['ferret', 'star', 'triangle', 'circle', 'square'];
Token.COLORS  = ['red', 'blue', 'green', 'pink', 'yellow'];
